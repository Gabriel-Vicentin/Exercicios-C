#include <stdlib.h>
#include <stdio.h>
#include <stdalign.h>

    int main (){

        float num, cont=0, nat;

            printf("Quantos numeros gostaria de testar? ");
                scanf("%f", &num);

            for ( cont = 0; cont < num; cont++)
            {
                if (num >= 0)
                    nat = num;
            }
            
    }