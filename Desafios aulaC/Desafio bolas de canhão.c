#include <stdio.h>
#include <stdlib.h>
#include <math.h>

    int main () {

        int num, lados, base;
        char fig;

            printf("Qual vai ser a figura utilizada? [Q para quadrado ou T para triangulo]");
                scanf("%s", &fig);

            if (lados < 3 || lados > 4){
                printf("Numero de lados invalido");
            }
            switch(fig){

            case 'Q':
            
            
            case 'Q':
                base = pow(lados, 2);

            }



    }